apiVersion: v1
kind: Service
metadata:
  name: cloudshift-web
  namespace: cloudshift
  labels:
    app: cloudshift-web
spec:
  type: ClusterIP
  selector:
    app: cloudshift-web
  ports:
    - name: http
      port: 80
      targetPort: http
